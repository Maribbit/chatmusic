# Player Components

This folder contains player-owned Lit custom elements and the small adapter functions that connect those elements to `renderer.ts`.

Use this folder for UI controls such as source visibility, fullscreen, keyboard visibility, quality feedback, tempo, duration, and the on-screen keyboard. Keep ABC parsing, host-page detection, and abcjs score rendering outside this folder.