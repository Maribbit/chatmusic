# Player Exports

This folder contains player-specific export helpers.

Use this folder for exporting rendered player artifacts such as SVG score images. Shared ABC file or MIDI export logic belongs in `src/shared/` when it is also useful outside the player.