# ChatMusic 开发进度与未来路线图 (Roadmap)

## 1. 项目当前进度 (Current Status)
目前 ChatMusic 的核心基础设施和主要功能已被完整跑通，并在不断迭代中从一个“能够显示乐谱的浏览器插件”进化成了一个“完整的 AI 音乐工作流解决方案”。

**已完成的核心里程碑：**
* **核心渲染与播放**：基于 `abcjs` 实现 ABC 代码的实时渲染和音频播放，修复了滚动布局和生命周期清理等隐患。
* **双端协同架构**：成功构建了 Chrome Extension 与挂载在网页上的独立站点（Studio）双端 Vite 构建方案。
* **独立工作区 (Studio)**：支持代码编辑与实时预览的分屏体验，并与网页聊天上下文打通（支持直接将聊天记录中的乐谱扔进 Studio）。
* **格式流转与工具链**：聚焦 ABC 本地打开/保存、SVG/MIDI 一键提取，并通过外部转换器链接衔接 MusicXML/ABC 格式转换。
* **物理反馈交互体验**：内置了独立钢琴音源（FluidR3），实现了屏幕虚拟键盘与发声互动的正向反馈。
* **代码质量保障 (Quality Check)**：打通了内联预览与独立页面的 ABC 错误检测和智能反馈（提取 parser warnings 并可以一键复制以供粘贴给 AI 重建代码）。

---

## 2. 深入优化与未来特性规划 (Future Roadmap)

以下是对后续探索方向的潜力、成本与收益评估（按推荐优先级排序）。

### 2.1 用户体验与界面视觉升级 (UI Layout & Icons)
目前的 UI 主要采用基础原生元素实现，视觉层级和谐度和“现代质感”可进一步提升。
* **具体内容**：引入专业的图标集（如 Lucide），优化 Studio 和内联面板的留白与间距，优化响应式断点（特别是移动端竖屏状态），微调暗黑 (Dark)/明亮 (Light) 模式下的色彩对比度与动效。
* **开发成本**：⭐⭐ (中低。主要涉及 CSS 和 SVG 图标配置)
* **用户收益**：⭐⭐⭐⭐ (极其直观。优雅的工具界面能带来非常明确的情绪价值)
* **长期潜力**：⭐⭐⭐ (提升作品专业度，降低第一次使用的信任门槛)
* **优先级推荐**：**P0 (非常适合正式推流发布前完成)**

### 2.2 PWA 支持 (Progressive Web App - 针对 Studio 网页版)
使得 Web 版本的 Studio 能够像独立 App 一样安装至桌面或手机主屏，支持离线访问。
* **具体内容**：配置 `manifest.json` 与 Service Worker 完善本地缓存策略。尤其是缓存体积达 1-2MB 的 Soundfont，以达到秒开效果及允许离线修改、播放本地 ABC 文件。
* **开发成本**：⭐⭐ (中。Vite 中有成熟的 `vite-plugin-pwa` 可供快速集成)
* **用户收益**：⭐⭐⭐⭐ (使得 Web 版不再纯粹是个网页，而是一个生产力工具体验)
* **长期潜力**：⭐⭐⭐⭐ (有利于独立跨端分发)
* **优先级推荐**：**P1**

### 2.3 外部转换工作流优化 (External MusicXML ↔ ABC Workflow)
ChatMusic 不再维护内建 MusicXML 转换器，而是将复杂格式转换交给成熟外部工具，自身专注 ABC 的编辑、检查、渲染和播放。
* **具体内容**：优化 Studio 中的外部转换器入口，整理 MusicXML/ABC 转换指南，支持用户从 xml2abc 等工具复制 ABC 后继续在 ChatMusic 中检查、排版和播放。
* **开发成本**：⭐ (低。主要是文档、链接入口和工作流说明)
* **用户收益**：⭐⭐⭐ (降低错误转换带来的误导，用户可以使用更成熟的转换器)
* **长期潜力**：⭐⭐⭐ (保持产品边界清晰，避免维护专业级格式转换器)
* **优先级推荐**：**P1 (持续打磨外部工具衔接，而不是重建转换器)**

### 2.4 国际化多语言支持 (i18n)
向海内外更广阔的市场拓宽覆盖。
* **具体内容**：界面文案、提示信息、扩展自带的 `description` ，以及“一键复制 AI 错误反馈 (Prompt)”的话术能够根据用户环境进行动态切换（默认支持 zh-CN, en-US）。
* **开发成本**：⭐⭐ (中。前端引入轻量 i18n 映射，扩展端接入 `_locales` 架构即可)
* **用户收益**：⭐⭐⭐ (非中文用户的入门必须条件)
* **长期潜力**：⭐⭐⭐⭐ (跨大语种用户基本盘，利于通过 Web Store 搜索自然增长)
* **优先级推荐**：**P2**

### 2.5 更专业的排版输出与播放控制
* **具体内容**：
  1. 支持分页和带页眉的 PDF 导出方案。
  2. 播放系统的音量条、节拍器、不同分轨声部选取发音等功能。
  3. 切换多乐器音源（如吉他、弦乐合奏等）。
* **开发成本**：⭐⭐⭐ (中高。寻找免费商用优质音源、精确分页均有一定难度)
* **用户收益**：⭐⭐ (特定情境下使用频率高)
* **长期潜力**：⭐⭐⭐ (偏向传统打谱软件侧)
* **优先级推荐**：**P2**

### 2.6 ABC 自愈与高级格式化 (Normalize & Repair)
目前已经通过反馈机制实现了“依赖 AI 来修谱”，下一步可以让宿主程序本身具有基础自愈能力。
* **具体内容**：开发真正的语义格式化程序。静默补齐缺失的 Header，标准化换行、去掉不支持的符号或转义语法，使得解析率再提升一个等级。
* **开发成本**：⭐⭐⭐⭐ (很高，相当于对 AST 进行深度改造)
* **用户收益**：⭐⭐⭐ (大幅降低报错率)
* **长期潜力**：⭐⭐⭐⭐ (核心技术壁垒)
* **优先级推荐**：**P3 (在目前“报错可贴回给 AI 修复”的基础下，可适当延后)**

### 2.6 这将带来完全基于乐谱的 AI 对话闭环 (AI Communication via Playable Interface)

使得计算机和人类能够跨越自然语言的壁垒，通过 MIDI 或麦克风作为真正的输入载体与 AI (LLMs) 进行“音乐会话”。
* **具体内容**：
  1. **单步乐谱输入 (Step-time MIDI / Click Input)**：利用 Web MIDI API 捕获带有 MIDI 的外部乐器信号，并与页面的屏幕键盘双向绑定。按下琴键则在编辑器光标处自动插入音符 ABC 代码（例如 C、D、E）。
  2. **麦克风输入与自动转谱 (Audio-to-Score / Transcription)**：结合前端 Web Audio API 和轻量级音高识别算法，捕捉单音哼唱或其他乐器的基频并转为音符符号输入给 AI。
* **开发成本**：⭐⭐⭐⭐ (较高。涉及对 Web Audio 与 Web MIDI 层的硬件授权、跨线程音频处理及一定的算法知识，特别是量化算法对齐节拍问题)
* **用户收益**：⭐⭐⭐⭐⭐ (颠覆式的功能。直接越过输入纯文本的门槛)
* **长期潜力**：⭐⭐⭐⭐⭐ (使该项目具备完整的“下一代音乐对话工具”特质，不再是一个简单的“播放渲染器”)
* **优先级推荐**：**P3 (远大愿景，在基础架构稳固后分阶段，从单步键盘输入作为切入点实现)**

---

## 3. 下阶段发布与迭代建议

1. **短期冲刺 (V1.0 发布版准备)**：
   优先把 `UI布局和图标 (2.1)` 梳理完，提升整套扩展产品的视觉完整性。这也是让第一波种子用户眼前一亮的重点。
2. **中期深耕 (生态外延)**：
   切入 `PWA (2.2)` 功能，强化独立的 Web 体验，并对中英文 `i18n (2.4)` 做好支持，为更大范围的受众开通渠道。
3. **长期积累 (技术壁垒)**：
   将 `外部转换工作流 (2.3)` 与 `标准化自修 (2.6)` 作为细水长流的任务，收集用户反馈里的 bad cases（坏用例），优先优化 ABC 侧的检查、排版和修复体验。
